name: 🚽 Zachodo Pridavka
on:
  repository_dispatch:
    inputs:
      filename:
        description: "File name, standart is timestamp"
        required: true
        default: "null"
      content:
        description: "JSON content with all data"
        reqiered: true
        default: ""

jobs:
  create-json:
    runs-on: ubuntu-latest
    steps:
    - run: _toilets/$.json < $
